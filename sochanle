#include "stdio.h" 
int main()
{ 
    //khai báo biến
    int songuyen;
    printf ("Nhap so nguyen: ");
    scanf ("%d", &songuyen); // lệnh để nhập vào
    if (songuyen % 2 == 0){ 
        printf("So nhap la so chan");
    }
    else{
        printf("So nhap la so le");
    }
}
